--- bot_old.py
+++ bot_new.py
@@ -148,28 +148,23 @@
         try:
             desktop = pathlib.Path.home() / "Desktop"
             self.main_folder = desktop / "TruckService_Manager"
             
-            # ОСНОВНЫЕ ПАПКИ
+            # ОСНОВНЫЕ ПАПКИ (УПРОЩЕННАЯ СТРУКТУРА)
             essential_folders = [
                 self.main_folder,
                 self.main_folder / "Шаблоны",
                 self.main_folder / "Общий_учет",
-                self.main_folder / "Логи"
+                self.main_folder / "Логи",
+                self.main_folder / "Пользовательские_списки"
             ]
             
             for folder in essential_folders:
                 folder.mkdir(exist_ok=True)
             
-            self.sections: Dict[str, Dict[str, Any]] = {
-                'mercedes': {
-                    'name': '🚗 Кузовные работы - Mercedes',
-                    'folder': self.main_folder / "Кузовные_работы_Mercedes",
-                    'works_file': "works_list_mercedes.xlsx"
-                },
-                'techosmotr': {
-                    'name': '🔧 Техническое обслуживание', 
-                    'folder': self.main_folder / "Техническое_обслуживание",
-                    'works_file': "works_list_techosmotr.xlsx"
+            # ✅ УПРОЩЕННАЯ СТРУКТУРА: 1 БАЗОВЫЙ РАЗДЕЛ
+            self.sections: Dict[str, Dict[str, Any]] = {
+                'base': {
+                    'name': '📋 Типовой заказ-наряд',
+                    'folder': self.main_folder / "Типовой_заказ",
+                    'works_file': "works_list_base.xlsx"
                 }
             }
             
-            # ПАПКИ РАЗДЕЛОВ - БЕЗ ПАПКИ "ШАБЛОНЫ"
-            for section_id, section_data in self.sections.items():
-                section_folders = [
-                    section_data['folder'],
-                    section_data['folder'] / "Заказы", 
-                    section_data['folder'] / "Учет",
-                    section_data['folder'] / "Фото",
-                    section_data['folder'] / "cache"
-                ]
-                
-                for folder in section_folders:
-                    folder.mkdir(parents=True, exist_ok=True)
-                
-                print(f"📁 Создана структура папок для: {section_data['name']}")
+            # ПАПКИ БАЗОВОГО РАЗДЕЛА
+            base_folders = [
+                self.sections['base']['folder'],
+                self.sections['base']['folder'] / "Заказы", 
+                self.sections['base']['folder'] / "Учет",
+                self.sections['base']['folder'] / "Фото",
+                self.sections['base']['folder'] / "cache"
+            ]
+            
+            for folder in base_folders:
+                folder.mkdir(parents=True, exist_ok=True)
+            
+            print(f"📁 Создана структура папок для: {self.sections['base']['name']}")
